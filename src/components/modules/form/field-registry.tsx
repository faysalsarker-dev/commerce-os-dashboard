// Field dispatch lives in EntityFormField so TypeScript can narrow each config
// before it reaches its specialised component. This module is intentionally kept
// as a stable import path for existing consumers.
export {}
